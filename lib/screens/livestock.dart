
import 'package:flutter/material.dart';

class LivestockScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Livestock')),
      body: Center(child: Text('Manage your livestock here')),
    );
  }
}
